model SportType {
  id          String   @id @default(cuid())
  title       String
  icon  String
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt
  
  turfs       Turf[]

  @@map("sport_type")
}
