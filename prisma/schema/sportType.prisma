model SportType {
  id        String   @id @default(uuid(7))
  title     String   @unique
  icon      String?  @unique
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  turfs Turf[]

  @@map("sport_type")
}
