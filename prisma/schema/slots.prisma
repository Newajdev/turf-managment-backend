model MasterSlot {
  id        String @id @default(cuid())
  slotType SlotType
  startTime String
  endTime   String
  duration  Int?

  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  turfSlots TurfSlot[]

  @@map("master_slot")
}
